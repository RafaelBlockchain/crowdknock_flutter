{
  "name": "crowdknock-backend",
  "version": "1.0.0",
  "description": "Backend API para CrowdKnock con Express y PostgreSQL",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "reset-db": "node scripts/reset.js"
  },

  "scripts": {
  "start": "node src/server.js",
  "dev": "nodemon src/server.js",
  "test": "jest",
  "test:watch": "jest --watch",
  "test:coverage": "jest --coverage"
}

  "scripts": {
  "test": "cross-env NODE_ENV=test jest --runInBand",
  "test:db-reset": "node scripts/resetTestDb.js"
}

  "scripts": {
  "test:create-db": "node scripts/createTestDb.js",
  "test:db-reset": "node scripts/resetTestDb.js",
  "test:full": "npm run test:create-db && npm run test:db-reset && npm test"
}

  "keywords": [
    "express",
    "api",
    "postgresql",
    "jwt",
    "crowdknock"
  ],
  "author": "Tu Nombre o Equipo",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"

  "scripts": {
  "sync-db": "node scripts/syncModels.js",
  ...

  "scripts": {
  "test": "cross-env NODE_ENV=test jest --detectOpenHandles"
},
"jest": {
  "testEnvironment": "node",
  "setupFilesAfterEnv": ["<rootDir>/tests/setup.js"]
      }
    } 
  }
}

