import 'package:flutter/material.dart';

class AppColors {
  // Primarios
  static const Color primary = Color(0xFF1A237E);     // Azul oscuro (Indigo 900)
  static const Color secondary = Color(0xFF00ACC1);   // Cyan 600

  // Grises
  static const Color background = Color(0xFFF4F6F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE0E0E0);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Estados
  static const Color success = Color(0xFF43A047);   // Green 600
  static const Color warning = Color(0xFFFDD835);   // Yellow 600
  static const Color danger = Color(0xFFE53935);    // Red 600
  static const Color info = Color(0xFF039BE5);      // Blue 600

  // Opacidad y hover
  static const Color primaryLight = Color(0xFF534BAE); // Indigo claro
  static const Color disabled = Color(0xFFBDBDBD);

  // Otros
  static const Color card = Color(0xFFFFFFFF);
  static const Color scaffold = Color(0xFFF5F5F5);
}

